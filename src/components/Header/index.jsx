import React from "react";
import { Button, Container, Title } from "./Header";

const Header = () => {
  return (
    <Container>
      <Title>Todo ReactJS + Firebase</Title>
      <Button>Sair</Button>
    </Container>
  );
};

export default Header;
